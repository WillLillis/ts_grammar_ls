grammar {
    language: "test",
}

let extras: list_t<rule_t> = [
    // whitespace
    regexp(r"\s"),
    // line comments
    comment,
]

rule program { "x" }

rule comment { token(seq("//", regexp("[^\n]*"))) }
