grammar {
    language: "test",
}

let PREC = { ADD: 1, MUL: 2, CALL: 8 }
let extras: list_rule_t = [regexp(r"\s"), comment]

rule program { "x" }

rule comment { token(seq("//", regexp("[^\n]*"))) }
